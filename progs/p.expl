int main()
{

 decl
   int a,b,c;
 enddecl
 read(a);
 read(b);
 read(c);
 if (a < b) then
  if (b < c) then write(c); else write(b); endif; 
 else
  if (a < c) then write(c); else write(a); endif; 
 endif; 
 return 0;
}
